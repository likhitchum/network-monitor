{
  "name": "wwnm-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright E2E tests for WinsWiew Network Monitor (run against a live docker compose stack)",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
